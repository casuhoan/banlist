services:
  banlist-app:
    build: .
    image: ghcr.io/${GITHUB_REPOSITORY:-tuo-username/banlist}:latest
    container_name: mtg-banlist
    ports:
      - "8083:80"
    volumes:
      - banlist_data:/app/data
    restart: unless-stopped

volumes:
  banlist_data:
